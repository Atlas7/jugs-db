{
  "name": "touch-tag-stats-db",
  "version": "1.0.0",
  "description": "A fake JSON DB to represent the touch tag tugb seasonal and game stats",
  "main": "index.js",
  "scripts": {
    "start-api": "json-server --watch db.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "json-server": "^0.12.1"
  }
}
